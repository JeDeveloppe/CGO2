{% extends 'base.html.twig' %}

{% block title %}ColorShop{% endblock %}

{% block body %}
    <h1>ColorShop</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ color_shop.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ color_shop.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_color_shop_index') }}">back to list</a>

    <a href="{{ path('app_color_shop_edit', {'id': color_shop.id}) }}">edit</a>

    {{ include('color_shop/_delete_form.html.twig') }}
{% endblock %}
